import {
  createProduct,
  getProducts,
  updateProduct,
  deleteProduct,
} from "../../services/productsManager.service.js";

const productManager = {
  createProduct,
  getProducts,
  updateProduct,
  deleteProduct,
};

export default productManager;
